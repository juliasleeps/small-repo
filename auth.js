export function login(user, pass) { return true; }
export function validateToken(token) { return git commit -m feat: add logout functiontoken; }
export function logout() { return true; }

// added from another user
